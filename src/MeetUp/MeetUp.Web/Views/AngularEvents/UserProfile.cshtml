@{
    Layout = null;
}

<div class="bordered">
    <div>
        <img class="profileimg" data-ng-src="{{user.ProfilePic}}" width="100%" title="{{user.Bio}}"/>
    </div>
    <div class="text-center text-primary" style="background-color: #428bca; color: #ffffff">
        {{user.Name}}
    </div>
</div>